<%- include('partials/header.ejs') -%>

    <section class="hero">
        <div class="container">
            <h1>分享生活，紀錄技術</h1>
            <p>歡迎來到我的部落格，這裡紀錄了我的學習筆記與生活點滴。</p>
        </div>
    </section>

    <div class="container main-layout">

        <main class="post-list">
            <% posts.forEach(post=> { %>
                <article class="card">
                    <div class="card-image" style="background-image: url('<%= post.image %>');">
                    </div>
                    <div class="card-content">
                        <span class="category">
                            <%= post.category %>
                        </span>
                        <h2><a href="#">
                                <%= post.title %>
                            </a></h2>
                        <form action="/delete/<%= post.id %>" method="POST" style="display:inline;">
                            <button type="submit" class="btn-delete" onclick="return confirm('確定要刪除這篇文章嗎？');">
                                ❌
                            </button>
                        </form>
                        <p class="excerpt">
                            <%= post.excerpt %>
                        </p>
                        <div class="meta">
                            <%= post.date %>
                        </div>
                    </div>
                </article>
                <% }); %>
        </main>

        <aside class="sidebar">
            <div class="widget">
                <h3>關於作者</h3>
                <img src="images/headshot.jpg" alt="作者頭像" class="avatar">
                <p>你好！我是 Lihsun，一名還在上學的軟體工程師。喜歡打羽球、學習與打game。</p>
            </div>

            <div class="widget">
                <h3>熱門標籤</h3>
                <div class="tags">
                    <a href="#">HTML5</a>
                    <a href="#">CSS3</a>
                    <a href="#">JavaScript</a>
                    <a href="#">React</a>
                    <a href="#">生活</a>
                </div>
            </div>
        </aside>

    </div>

    <%- include("partials/footer.ejs") %>